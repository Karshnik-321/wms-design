// $((function(){"use strict";$(".invoice-repeater, .repeater-default").repeater({show:function(){$(this).slideDown(),$(".list-group aside:last-child .form-control").removeAttr("value"),feather&&feather.replace({width:14,height:14})},hide:function(e){confirm("Are you sure you want to delete this element?")&&$(this).slideUp(e)}})}));



$(function () {
  'use strict';

  // form repeater jquery
  $('.invoice-repeater, .repeater-default').repeater({
    show: function () {
      $(this).slideDown();
      $(".list-group aside:last-child .form-control").removeAttr("value");
      // Feather Icons
      if (feather) {
        feather.replace({ width: 14, height: 14 });
      }
    },
    hide: function (deleteElement) {
      if (confirm('Are you sure you want to delete this element?')) {
        $(this).slideUp(deleteElement);
      }
    }
  });
});


 